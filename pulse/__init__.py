"""Importable core: panel formatting and the per-agent transcript readers.

Everything runnable lives at the repository root instead - the hook entry
points are referenced by absolute path from the agents' config files, so they
are easier to find there.
"""
